version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: shipment-delay-api
    ports:
      - "8000:8000"
    restart: unless-stopped

  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    container_name: shipment-delay-dashboard
    ports:
      - "8501:8501"
    depends_on:
      - api
    restart: unless-stopped
